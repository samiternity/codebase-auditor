export async function onRequest(context) { const { request } = context; const url = new URL(request.url); const targetUrl = 'http://13.53.245.246:8000' + url.pathname + url.search; const newRequest = new Request(targetUrl, request); try { return await fetch(newRequest); } catch(err) { return new Response('Proxy Error: ' + err.message, {status:502}); } }